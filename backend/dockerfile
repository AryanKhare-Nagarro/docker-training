# Use the official Node.js 18 alpine lightweight image as the base image
FROM node:18-alpine

# Set the working directory inside the container to /app/backend
WORKDIR /app/backend

# Copy package.json and package-lock.json to install dependencies
COPY package*.json ./

# Install the application dependencies
RUN npm install

# Copy the rest of the application code to the working directory
COPY . .

# Build the application and expose port 3000 to allow external access
EXPOSE 3000

# Start the application using the command "node index.js"
CMD [ "node", "index.js" ]